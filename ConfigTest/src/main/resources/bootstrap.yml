spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JoseAlbertoDev/configServerTest.git
          search-paths:
          - '{application}'
          
management:
  security:
    enabled: false

server:
  port: 8000
